/* Estilos generales */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

h1 {
  text-align: center;
  margin-bottom: 30px;
}

/* Formulario */
form {
  display: flex;
  flex-direction: column;
  gap: 10px;
  max-width: 400px;
  margin: 0 auto;
}

input,
select,
button {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

button {
  cursor: pointer;
  background-color: #007BFF;
  color: white;
  border: none;
  transition: background-color 0.3s ease;
}

button:hover {
  background-color: #0056b3;
}

/* Estilo para el contenedor de items */
.items {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 20px;
  margin-top: 40px;
}

.item {
  background-color: #222;
  color: white;
  padding: 20px;
  border-radius: 10px;
  position: relative;
  overflow: hidden;
  box-sizing: border-box;
  text-align: left; /* Alineación a la izquierda */
  display: flex;
  flex-direction: column; /* Cambiar a columna */
  justify-content: flex-start;
  height: auto;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* Hover para el contenedor de cada item */
.item:hover {
  transform: scale(1.05); /* Aumentar ligeramente el tamaño */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Sombra al pasar el cursor */
}

/* Título de la película más grande */
.item span strong {
  font-size: 1.5em; /* Aumentar tamaño del título */
  margin-bottom: 10px; /* Separar el título del resto del contenido */
  display: block;
}

/* Sinopsis */
.item span.sinopsis {
  display: block;
  margin-bottom: 10px;
  line-height: 1.5;
  max-height: 120px;
  overflow: hidden;
  text-overflow: ellipsis;
  position: relative;
}

/* Efecto difuminado en la sinopsis cuando el texto es muy largo */
.item span.sinopsis:after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 30px;
  background: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, 1));
}

/* Fecha de lanzamiento y director */
.item .fecha-lanzamiento,
.item .director {
  font-size: 1em;
  margin-bottom: 10px;
}

/* Botones de edición y eliminación */
.item-buttons {
  display: flex;
  gap: 10px;
  margin-top: 10px;
}

.edit-btn,
.delete-btn {
  padding: 5px 10px;
  background-color: #28a745;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: opacity 0.3s ease;
}

.edit-btn {
  background-color: #007BFF;
}

.delete-btn {
  background-color: #d33;
}

/* Hover para los botones */
.edit-btn:hover,
.delete-btn:hover {
  opacity: 0.8;
}

.edit-btn:active,
.delete-btn:active {
  transform: scale(0.95); /* Efecto de pulsación */
}

/* Estilo para los inputs, select y botones */
input,
select,
button {
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  transition: all 0.3s ease; /* Agregar transición para el cambio suave */
}

/* Efecto cuando el input o select está enfocado */
input:focus,
select:focus {
  border-color: #007BFF; /* Cambiar el color del borde */
  outline: none; /* Eliminar el contorno predeterminado */
  box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Sombra sutil al enfoque */
}

/* Estilo para el botón */
button {
  cursor: pointer;
  background-color: #007BFF;
  color: white;
  border: none;
  transition: background-color 0.3s ease, transform 0.2s ease; /* Transición para color y movimiento */
}

button:hover {
  background-color: #0056b3;
  transform: translateY(-4px); /* Mover el botón ligeramente hacia arriba */
}

/* Efecto de brinquito al hacer clic */
button:active {
  transform: translateY(2px); /* Mover el botón hacia abajo ligeramente al hacer clic */
}

/* Estilo para el hover en inputs y select */
input:hover,
select:hover {
  border-color: #0056b3; /* Cambiar color del borde en hover */
}